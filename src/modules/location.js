export default class Location {
    constructor(id, text, imgSrc, bgColor, directions) {
        this.id = id
        this.text = text
        this.imgSrc = imgSrc
        this.bgColor = bgColor
        this.directions = directions
    }
}