export default class Location {
    constructor(id, text, imgSrc, bgColor, directions, items) {
        this.id = id
        this.text = text
        this.imgSrc = imgSrc
        this.bgColor = bgColor
        this.directions = directions
        this.items = items
    }
}